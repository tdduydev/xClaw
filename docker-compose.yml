version: '3.8'

services:
  autox:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - autox-data:/app/data
    restart: unless-stopped

volumes:
  autox-data:
